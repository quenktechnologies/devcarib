%import adminCtl from "./handlers"

GET     /               adminCtl.showIndex

GET     /r/posts        adminCtl.setQuery adminCtl.search

PATCH   /r/posts/:id    adminCtl.update

GET     /r/posts/:id    adminCtl.get

DELETE  /r/posts/:id    adminCtl.remove
