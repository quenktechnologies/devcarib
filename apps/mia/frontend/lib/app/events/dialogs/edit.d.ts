import { AddEventDialog } from './add';
/**
 * EditEventDialog provides a form embedded in a dialog for editing existing users.
 */
export declare class EditEventDialog extends AddEventDialog {
    name: string;
    mode: string;
}
