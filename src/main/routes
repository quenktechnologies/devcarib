%import showIndex,
        showRegistrationForm,
        showLoginForm,
        showDashboard,
        login,
        logout,
        createEmployer,
        createJob 
from "./handlers"

GET / showIndex

GET /register showRegistrationForm

POST /register  createEmployer

GET /login showLoginForm

POST /login login

GET /logout logout

GET /dashboard showDashboard

POST /api/jobs createJob
